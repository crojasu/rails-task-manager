<h1>HELLO TASKS</h1>

<ul>
  <%  @task.each do |task|%>
  <li>
 <%= link_to task.title, task_path(task) %>
 ||
 <%= link_to "edit", edit_task_path(task) %>
 ||
 <i class="fa fa-trash-o" aria-hidden="true"> </i>
  <%= link_to "eli", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>

</li>
<% end %>
</ul>
 <%= link_to 'new', new_task_path %>
